# 2026-07-10 工作日志

## Piper 离线语音 — 完成 ✔
- ✅ 下载源从 HuggingFace 改为 GitHub release（国内可直连）
- ✅ 修复 `BytesIO` 缺少 `seek(0)` 导致 tar.gz 读取为空的 bug
- ✅ 中文 x-low 音色（18MB）下载成功：`default.onnx` 19.7MB + `default.onnx.json`
- ✅ Piper TTS 合成验证通过：73KB WAV，`audio/wav`，中文内容
- ✅ 可用音色：`zh_CN-huayan-x-low`（18MB）和 `zh_CN-huayan-medium`（57MB）
- ✅ 后台异步下载 + 前端轮询进度（每 2 秒）

## 自定义音色（语音克隆）— 受阻 ❌
- **根因**：`TTS==0.22.0` 要求 Python 3.9-3.11，且只有 Linux wheel（无 Windows 包）
- 项目 venv 是 Python 3.13，不兼容
- 已下载 Python 3.11.9 embeddable 到 `C:\Users\OMEN\.workbuddy\binaries\python\versions\3.11.9\`（有 pip）
- 但沙箱网络慢，PyTorch ~800MB 无法在此完成
- **替代方案**：用 Python 3.11 的 sub-venv，主服务调子进程
- app.py 错误提示已更新，说明需要 Python 3.11

## 其他修复
1. ✅ start.bat 编码（BOM 导致 cmd 乱码）——纯 ASCII + chr(92)
2. ✅ coqui-tts 依赖冲突 → 改 TTS==0.22.0（但需 Python 3.11）
3. ✅ Piper 下载源 HuggingFace → GitHub release
4. ✅ Piper 下载改为后台异步 + 前端进度轮询 + `&lt;/api/tts/piper/status&gt;`
5. ✅ start.bat 启动时自动检测 TTS 安装状态
