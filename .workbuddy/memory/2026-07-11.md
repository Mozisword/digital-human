# 2026-07-11 工作日志

## 自定义音色（语音克隆）安装 — PyTorch 下载中
- **coqui-tts==0.27.5 已安装** ✅（idiap 社区 fork，纯 Python wheel 兼容 Windows）
- **num2words 已安装** ✅（需从官方 PyPI 装，阿里云镜像没有）
- **PyTorch CPU 正在后台装**（task: IEj6b9，~300MB）
- `from TTS.api import TTS` 在 PyTorch 装完后就可用了
- requirements.txt → `coqui-tts==0.27.5` + `librosa`
- app.py 错误提示 → `coqui-tts==0.27.5`
- start.bat 自动检测 → `coqui-tts==0.27.5`

## 数字人听懂"唱歌"指令 🎵
- **功能**：对话中说"唱首歌"或"唱一首歌"，数字人自动从歌库找歌播放
- **实现**：
  - 系统提示词动态注入歌单（`buildSystemPrompt()`）
  - LLM 回复带 `🎵 唱首歌: 歌名` 标记，前端 auto-detect 并播歌
  - `cachedSongTitles` 缓存，歌库打开时清空以刷新
  - 兼容 `/sing 歌名` 命令
- **文件**：`static/app.js` — `buildSystemPrompt`、`sendMessage` 检测 + playback

## 已验证正常
- Piper 离线 TTS ✅（中文合成 73KB WAV）
- edge-tts 在线 TTS ✅
- Ollama 流式对话 ✅
- 歌库 + 唱歌 ✅（现在可说说话唱歌）
- start.bat 编码 ✅（纯 ASCII + chr(92)）
